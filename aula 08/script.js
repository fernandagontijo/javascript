// Atividade 01
 //Crie um arquivo JavaScript e implemente a função getValue que irá pegar
 //o valor do input e exibi-lo no console

 function getValue() {
    // Seleciona o input pelo ID (por exemplo, "meuInput")
    const inputElement = document.getElementById('meuInput');
    // Pega o valor do input
    const inputValue = inputElement.value;
    // Exibe o valor no console
    console.log(inputValue);
}

